# API base URL (update to your machine's local IP when testing on a physical device)
# Example for physical device on same WiFi: EXPO_PUBLIC_API_URL=http://192.168.1.42:3000
# For simulator/emulator: http://localhost:3000 works as-is via app.json extra.apiUrl
EXPO_PUBLIC_API_URL=http://localhost:3000
